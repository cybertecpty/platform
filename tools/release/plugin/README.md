# release-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build release-plugin` to build the library.

## Running unit tests

Run `nx test release-plugin` to execute the unit tests via [Jest](https://jestjs.io).
